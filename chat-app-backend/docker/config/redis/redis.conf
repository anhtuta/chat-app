# Redis configuration file
# This file is mounted into the Redis container

# Enable AOF (Append Only File) persistence
appendonly yes

# AOF file name
appendfilename "appendonly.aof"

# Directory for AOF file (mapped to /data in container)
dir /data

# Note: Password is set via command line to use environment variable
# This allows dynamic password configuration

